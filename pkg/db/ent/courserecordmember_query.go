// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"saas/pkg/db/ent/courserecordmember"
	"saas/pkg/db/ent/courserecordschedule"
	"saas/pkg/db/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CourseRecordMemberQuery is the builder for querying CourseRecordMember entities.
type CourseRecordMemberQuery struct {
	config
	ctx          *QueryContext
	order        []courserecordmember.OrderOption
	inters       []Interceptor
	predicates   []predicate.CourseRecordMember
	withSchedule *CourseRecordScheduleQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CourseRecordMemberQuery builder.
func (crmq *CourseRecordMemberQuery) Where(ps ...predicate.CourseRecordMember) *CourseRecordMemberQuery {
	crmq.predicates = append(crmq.predicates, ps...)
	return crmq
}

// Limit the number of records to be returned by this query.
func (crmq *CourseRecordMemberQuery) Limit(limit int) *CourseRecordMemberQuery {
	crmq.ctx.Limit = &limit
	return crmq
}

// Offset to start from.
func (crmq *CourseRecordMemberQuery) Offset(offset int) *CourseRecordMemberQuery {
	crmq.ctx.Offset = &offset
	return crmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (crmq *CourseRecordMemberQuery) Unique(unique bool) *CourseRecordMemberQuery {
	crmq.ctx.Unique = &unique
	return crmq
}

// Order specifies how the records should be ordered.
func (crmq *CourseRecordMemberQuery) Order(o ...courserecordmember.OrderOption) *CourseRecordMemberQuery {
	crmq.order = append(crmq.order, o...)
	return crmq
}

// QuerySchedule chains the current query on the "schedule" edge.
func (crmq *CourseRecordMemberQuery) QuerySchedule() *CourseRecordScheduleQuery {
	query := (&CourseRecordScheduleClient{config: crmq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := crmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := crmq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(courserecordmember.Table, courserecordmember.FieldID, selector),
			sqlgraph.To(courserecordschedule.Table, courserecordschedule.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, courserecordmember.ScheduleTable, courserecordmember.ScheduleColumn),
		)
		fromU = sqlgraph.SetNeighbors(crmq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CourseRecordMember entity from the query.
// Returns a *NotFoundError when no CourseRecordMember was found.
func (crmq *CourseRecordMemberQuery) First(ctx context.Context) (*CourseRecordMember, error) {
	nodes, err := crmq.Limit(1).All(setContextOp(ctx, crmq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{courserecordmember.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (crmq *CourseRecordMemberQuery) FirstX(ctx context.Context) *CourseRecordMember {
	node, err := crmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CourseRecordMember ID from the query.
// Returns a *NotFoundError when no CourseRecordMember ID was found.
func (crmq *CourseRecordMemberQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = crmq.Limit(1).IDs(setContextOp(ctx, crmq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{courserecordmember.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (crmq *CourseRecordMemberQuery) FirstIDX(ctx context.Context) int64 {
	id, err := crmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CourseRecordMember entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CourseRecordMember entity is found.
// Returns a *NotFoundError when no CourseRecordMember entities are found.
func (crmq *CourseRecordMemberQuery) Only(ctx context.Context) (*CourseRecordMember, error) {
	nodes, err := crmq.Limit(2).All(setContextOp(ctx, crmq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{courserecordmember.Label}
	default:
		return nil, &NotSingularError{courserecordmember.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (crmq *CourseRecordMemberQuery) OnlyX(ctx context.Context) *CourseRecordMember {
	node, err := crmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CourseRecordMember ID in the query.
// Returns a *NotSingularError when more than one CourseRecordMember ID is found.
// Returns a *NotFoundError when no entities are found.
func (crmq *CourseRecordMemberQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = crmq.Limit(2).IDs(setContextOp(ctx, crmq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{courserecordmember.Label}
	default:
		err = &NotSingularError{courserecordmember.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (crmq *CourseRecordMemberQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := crmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CourseRecordMembers.
func (crmq *CourseRecordMemberQuery) All(ctx context.Context) ([]*CourseRecordMember, error) {
	ctx = setContextOp(ctx, crmq.ctx, "All")
	if err := crmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CourseRecordMember, *CourseRecordMemberQuery]()
	return withInterceptors[[]*CourseRecordMember](ctx, crmq, qr, crmq.inters)
}

// AllX is like All, but panics if an error occurs.
func (crmq *CourseRecordMemberQuery) AllX(ctx context.Context) []*CourseRecordMember {
	nodes, err := crmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CourseRecordMember IDs.
func (crmq *CourseRecordMemberQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if crmq.ctx.Unique == nil && crmq.path != nil {
		crmq.Unique(true)
	}
	ctx = setContextOp(ctx, crmq.ctx, "IDs")
	if err = crmq.Select(courserecordmember.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (crmq *CourseRecordMemberQuery) IDsX(ctx context.Context) []int64 {
	ids, err := crmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (crmq *CourseRecordMemberQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, crmq.ctx, "Count")
	if err := crmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, crmq, querierCount[*CourseRecordMemberQuery](), crmq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (crmq *CourseRecordMemberQuery) CountX(ctx context.Context) int {
	count, err := crmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (crmq *CourseRecordMemberQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, crmq.ctx, "Exist")
	switch _, err := crmq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (crmq *CourseRecordMemberQuery) ExistX(ctx context.Context) bool {
	exist, err := crmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CourseRecordMemberQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (crmq *CourseRecordMemberQuery) Clone() *CourseRecordMemberQuery {
	if crmq == nil {
		return nil
	}
	return &CourseRecordMemberQuery{
		config:       crmq.config,
		ctx:          crmq.ctx.Clone(),
		order:        append([]courserecordmember.OrderOption{}, crmq.order...),
		inters:       append([]Interceptor{}, crmq.inters...),
		predicates:   append([]predicate.CourseRecordMember{}, crmq.predicates...),
		withSchedule: crmq.withSchedule.Clone(),
		// clone intermediate query.
		sql:  crmq.sql.Clone(),
		path: crmq.path,
	}
}

// WithSchedule tells the query-builder to eager-load the nodes that are connected to
// the "schedule" edge. The optional arguments are used to configure the query builder of the edge.
func (crmq *CourseRecordMemberQuery) WithSchedule(opts ...func(*CourseRecordScheduleQuery)) *CourseRecordMemberQuery {
	query := (&CourseRecordScheduleClient{config: crmq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	crmq.withSchedule = query
	return crmq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CourseRecordMember.Query().
//		GroupBy(courserecordmember.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (crmq *CourseRecordMemberQuery) GroupBy(field string, fields ...string) *CourseRecordMemberGroupBy {
	crmq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CourseRecordMemberGroupBy{build: crmq}
	grbuild.flds = &crmq.ctx.Fields
	grbuild.label = courserecordmember.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.CourseRecordMember.Query().
//		Select(courserecordmember.FieldCreatedAt).
//		Scan(ctx, &v)
func (crmq *CourseRecordMemberQuery) Select(fields ...string) *CourseRecordMemberSelect {
	crmq.ctx.Fields = append(crmq.ctx.Fields, fields...)
	sbuild := &CourseRecordMemberSelect{CourseRecordMemberQuery: crmq}
	sbuild.label = courserecordmember.Label
	sbuild.flds, sbuild.scan = &crmq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CourseRecordMemberSelect configured with the given aggregations.
func (crmq *CourseRecordMemberQuery) Aggregate(fns ...AggregateFunc) *CourseRecordMemberSelect {
	return crmq.Select().Aggregate(fns...)
}

func (crmq *CourseRecordMemberQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range crmq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, crmq); err != nil {
				return err
			}
		}
	}
	for _, f := range crmq.ctx.Fields {
		if !courserecordmember.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if crmq.path != nil {
		prev, err := crmq.path(ctx)
		if err != nil {
			return err
		}
		crmq.sql = prev
	}
	return nil
}

func (crmq *CourseRecordMemberQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CourseRecordMember, error) {
	var (
		nodes       = []*CourseRecordMember{}
		_spec       = crmq.querySpec()
		loadedTypes = [1]bool{
			crmq.withSchedule != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CourseRecordMember).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CourseRecordMember{config: crmq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, crmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := crmq.withSchedule; query != nil {
		if err := crmq.loadSchedule(ctx, query, nodes, nil,
			func(n *CourseRecordMember, e *CourseRecordSchedule) { n.Edges.Schedule = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (crmq *CourseRecordMemberQuery) loadSchedule(ctx context.Context, query *CourseRecordScheduleQuery, nodes []*CourseRecordMember, init func(*CourseRecordMember), assign func(*CourseRecordMember, *CourseRecordSchedule)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*CourseRecordMember)
	for i := range nodes {
		fk := nodes[i].CourseRecordScheduleID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(courserecordschedule.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "course_record_schedule_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (crmq *CourseRecordMemberQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := crmq.querySpec()
	_spec.Node.Columns = crmq.ctx.Fields
	if len(crmq.ctx.Fields) > 0 {
		_spec.Unique = crmq.ctx.Unique != nil && *crmq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, crmq.driver, _spec)
}

func (crmq *CourseRecordMemberQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(courserecordmember.Table, courserecordmember.Columns, sqlgraph.NewFieldSpec(courserecordmember.FieldID, field.TypeInt64))
	_spec.From = crmq.sql
	if unique := crmq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if crmq.path != nil {
		_spec.Unique = true
	}
	if fields := crmq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, courserecordmember.FieldID)
		for i := range fields {
			if fields[i] != courserecordmember.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if crmq.withSchedule != nil {
			_spec.Node.AddColumnOnce(courserecordmember.FieldCourseRecordScheduleID)
		}
	}
	if ps := crmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := crmq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := crmq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := crmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (crmq *CourseRecordMemberQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(crmq.driver.Dialect())
	t1 := builder.Table(courserecordmember.Table)
	columns := crmq.ctx.Fields
	if len(columns) == 0 {
		columns = courserecordmember.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if crmq.sql != nil {
		selector = crmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if crmq.ctx.Unique != nil && *crmq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range crmq.predicates {
		p(selector)
	}
	for _, p := range crmq.order {
		p(selector)
	}
	if offset := crmq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := crmq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CourseRecordMemberGroupBy is the group-by builder for CourseRecordMember entities.
type CourseRecordMemberGroupBy struct {
	selector
	build *CourseRecordMemberQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (crmgb *CourseRecordMemberGroupBy) Aggregate(fns ...AggregateFunc) *CourseRecordMemberGroupBy {
	crmgb.fns = append(crmgb.fns, fns...)
	return crmgb
}

// Scan applies the selector query and scans the result into the given value.
func (crmgb *CourseRecordMemberGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, crmgb.build.ctx, "GroupBy")
	if err := crmgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CourseRecordMemberQuery, *CourseRecordMemberGroupBy](ctx, crmgb.build, crmgb, crmgb.build.inters, v)
}

func (crmgb *CourseRecordMemberGroupBy) sqlScan(ctx context.Context, root *CourseRecordMemberQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(crmgb.fns))
	for _, fn := range crmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*crmgb.flds)+len(crmgb.fns))
		for _, f := range *crmgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*crmgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := crmgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CourseRecordMemberSelect is the builder for selecting fields of CourseRecordMember entities.
type CourseRecordMemberSelect struct {
	*CourseRecordMemberQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (crms *CourseRecordMemberSelect) Aggregate(fns ...AggregateFunc) *CourseRecordMemberSelect {
	crms.fns = append(crms.fns, fns...)
	return crms
}

// Scan applies the selector query and scans the result into the given value.
func (crms *CourseRecordMemberSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, crms.ctx, "Select")
	if err := crms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CourseRecordMemberQuery, *CourseRecordMemberSelect](ctx, crms.CourseRecordMemberQuery, crms, crms.inters, v)
}

func (crms *CourseRecordMemberSelect) sqlScan(ctx context.Context, root *CourseRecordMemberQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(crms.fns))
	for _, fn := range crms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*crms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := crms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
