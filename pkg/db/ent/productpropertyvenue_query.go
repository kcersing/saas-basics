// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"saas/pkg/db/ent/predicate"
	"saas/pkg/db/ent/productpropertyvenue"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ProductPropertyVenueQuery is the builder for querying ProductPropertyVenue entities.
type ProductPropertyVenueQuery struct {
	config
	ctx        *QueryContext
	order      []productpropertyvenue.OrderOption
	inters     []Interceptor
	predicates []predicate.ProductPropertyVenue
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProductPropertyVenueQuery builder.
func (ppvq *ProductPropertyVenueQuery) Where(ps ...predicate.ProductPropertyVenue) *ProductPropertyVenueQuery {
	ppvq.predicates = append(ppvq.predicates, ps...)
	return ppvq
}

// Limit the number of records to be returned by this query.
func (ppvq *ProductPropertyVenueQuery) Limit(limit int) *ProductPropertyVenueQuery {
	ppvq.ctx.Limit = &limit
	return ppvq
}

// Offset to start from.
func (ppvq *ProductPropertyVenueQuery) Offset(offset int) *ProductPropertyVenueQuery {
	ppvq.ctx.Offset = &offset
	return ppvq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ppvq *ProductPropertyVenueQuery) Unique(unique bool) *ProductPropertyVenueQuery {
	ppvq.ctx.Unique = &unique
	return ppvq
}

// Order specifies how the records should be ordered.
func (ppvq *ProductPropertyVenueQuery) Order(o ...productpropertyvenue.OrderOption) *ProductPropertyVenueQuery {
	ppvq.order = append(ppvq.order, o...)
	return ppvq
}

// First returns the first ProductPropertyVenue entity from the query.
// Returns a *NotFoundError when no ProductPropertyVenue was found.
func (ppvq *ProductPropertyVenueQuery) First(ctx context.Context) (*ProductPropertyVenue, error) {
	nodes, err := ppvq.Limit(1).All(setContextOp(ctx, ppvq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{productpropertyvenue.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ppvq *ProductPropertyVenueQuery) FirstX(ctx context.Context) *ProductPropertyVenue {
	node, err := ppvq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProductPropertyVenue ID from the query.
// Returns a *NotFoundError when no ProductPropertyVenue ID was found.
func (ppvq *ProductPropertyVenueQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = ppvq.Limit(1).IDs(setContextOp(ctx, ppvq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{productpropertyvenue.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ppvq *ProductPropertyVenueQuery) FirstIDX(ctx context.Context) int64 {
	id, err := ppvq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProductPropertyVenue entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProductPropertyVenue entity is found.
// Returns a *NotFoundError when no ProductPropertyVenue entities are found.
func (ppvq *ProductPropertyVenueQuery) Only(ctx context.Context) (*ProductPropertyVenue, error) {
	nodes, err := ppvq.Limit(2).All(setContextOp(ctx, ppvq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{productpropertyvenue.Label}
	default:
		return nil, &NotSingularError{productpropertyvenue.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ppvq *ProductPropertyVenueQuery) OnlyX(ctx context.Context) *ProductPropertyVenue {
	node, err := ppvq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProductPropertyVenue ID in the query.
// Returns a *NotSingularError when more than one ProductPropertyVenue ID is found.
// Returns a *NotFoundError when no entities are found.
func (ppvq *ProductPropertyVenueQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = ppvq.Limit(2).IDs(setContextOp(ctx, ppvq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{productpropertyvenue.Label}
	default:
		err = &NotSingularError{productpropertyvenue.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ppvq *ProductPropertyVenueQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := ppvq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProductPropertyVenues.
func (ppvq *ProductPropertyVenueQuery) All(ctx context.Context) ([]*ProductPropertyVenue, error) {
	ctx = setContextOp(ctx, ppvq.ctx, "All")
	if err := ppvq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProductPropertyVenue, *ProductPropertyVenueQuery]()
	return withInterceptors[[]*ProductPropertyVenue](ctx, ppvq, qr, ppvq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ppvq *ProductPropertyVenueQuery) AllX(ctx context.Context) []*ProductPropertyVenue {
	nodes, err := ppvq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProductPropertyVenue IDs.
func (ppvq *ProductPropertyVenueQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if ppvq.ctx.Unique == nil && ppvq.path != nil {
		ppvq.Unique(true)
	}
	ctx = setContextOp(ctx, ppvq.ctx, "IDs")
	if err = ppvq.Select(productpropertyvenue.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ppvq *ProductPropertyVenueQuery) IDsX(ctx context.Context) []int64 {
	ids, err := ppvq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ppvq *ProductPropertyVenueQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ppvq.ctx, "Count")
	if err := ppvq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ppvq, querierCount[*ProductPropertyVenueQuery](), ppvq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ppvq *ProductPropertyVenueQuery) CountX(ctx context.Context) int {
	count, err := ppvq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ppvq *ProductPropertyVenueQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ppvq.ctx, "Exist")
	switch _, err := ppvq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ppvq *ProductPropertyVenueQuery) ExistX(ctx context.Context) bool {
	exist, err := ppvq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProductPropertyVenueQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ppvq *ProductPropertyVenueQuery) Clone() *ProductPropertyVenueQuery {
	if ppvq == nil {
		return nil
	}
	return &ProductPropertyVenueQuery{
		config:     ppvq.config,
		ctx:        ppvq.ctx.Clone(),
		order:      append([]productpropertyvenue.OrderOption{}, ppvq.order...),
		inters:     append([]Interceptor{}, ppvq.inters...),
		predicates: append([]predicate.ProductPropertyVenue{}, ppvq.predicates...),
		// clone intermediate query.
		sql:  ppvq.sql.Clone(),
		path: ppvq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProductPropertyVenue.Query().
//		GroupBy(productpropertyvenue.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ppvq *ProductPropertyVenueQuery) GroupBy(field string, fields ...string) *ProductPropertyVenueGroupBy {
	ppvq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProductPropertyVenueGroupBy{build: ppvq}
	grbuild.flds = &ppvq.ctx.Fields
	grbuild.label = productpropertyvenue.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ProductPropertyVenue.Query().
//		Select(productpropertyvenue.FieldCreatedAt).
//		Scan(ctx, &v)
func (ppvq *ProductPropertyVenueQuery) Select(fields ...string) *ProductPropertyVenueSelect {
	ppvq.ctx.Fields = append(ppvq.ctx.Fields, fields...)
	sbuild := &ProductPropertyVenueSelect{ProductPropertyVenueQuery: ppvq}
	sbuild.label = productpropertyvenue.Label
	sbuild.flds, sbuild.scan = &ppvq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProductPropertyVenueSelect configured with the given aggregations.
func (ppvq *ProductPropertyVenueQuery) Aggregate(fns ...AggregateFunc) *ProductPropertyVenueSelect {
	return ppvq.Select().Aggregate(fns...)
}

func (ppvq *ProductPropertyVenueQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ppvq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ppvq); err != nil {
				return err
			}
		}
	}
	for _, f := range ppvq.ctx.Fields {
		if !productpropertyvenue.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ppvq.path != nil {
		prev, err := ppvq.path(ctx)
		if err != nil {
			return err
		}
		ppvq.sql = prev
	}
	return nil
}

func (ppvq *ProductPropertyVenueQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProductPropertyVenue, error) {
	var (
		nodes = []*ProductPropertyVenue{}
		_spec = ppvq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProductPropertyVenue).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProductPropertyVenue{config: ppvq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ppvq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ppvq *ProductPropertyVenueQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ppvq.querySpec()
	_spec.Node.Columns = ppvq.ctx.Fields
	if len(ppvq.ctx.Fields) > 0 {
		_spec.Unique = ppvq.ctx.Unique != nil && *ppvq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ppvq.driver, _spec)
}

func (ppvq *ProductPropertyVenueQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(productpropertyvenue.Table, productpropertyvenue.Columns, sqlgraph.NewFieldSpec(productpropertyvenue.FieldID, field.TypeInt64))
	_spec.From = ppvq.sql
	if unique := ppvq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ppvq.path != nil {
		_spec.Unique = true
	}
	if fields := ppvq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, productpropertyvenue.FieldID)
		for i := range fields {
			if fields[i] != productpropertyvenue.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ppvq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ppvq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ppvq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ppvq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ppvq *ProductPropertyVenueQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ppvq.driver.Dialect())
	t1 := builder.Table(productpropertyvenue.Table)
	columns := ppvq.ctx.Fields
	if len(columns) == 0 {
		columns = productpropertyvenue.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ppvq.sql != nil {
		selector = ppvq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ppvq.ctx.Unique != nil && *ppvq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ppvq.predicates {
		p(selector)
	}
	for _, p := range ppvq.order {
		p(selector)
	}
	if offset := ppvq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ppvq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProductPropertyVenueGroupBy is the group-by builder for ProductPropertyVenue entities.
type ProductPropertyVenueGroupBy struct {
	selector
	build *ProductPropertyVenueQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ppvgb *ProductPropertyVenueGroupBy) Aggregate(fns ...AggregateFunc) *ProductPropertyVenueGroupBy {
	ppvgb.fns = append(ppvgb.fns, fns...)
	return ppvgb
}

// Scan applies the selector query and scans the result into the given value.
func (ppvgb *ProductPropertyVenueGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ppvgb.build.ctx, "GroupBy")
	if err := ppvgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProductPropertyVenueQuery, *ProductPropertyVenueGroupBy](ctx, ppvgb.build, ppvgb, ppvgb.build.inters, v)
}

func (ppvgb *ProductPropertyVenueGroupBy) sqlScan(ctx context.Context, root *ProductPropertyVenueQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ppvgb.fns))
	for _, fn := range ppvgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ppvgb.flds)+len(ppvgb.fns))
		for _, f := range *ppvgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ppvgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ppvgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProductPropertyVenueSelect is the builder for selecting fields of ProductPropertyVenue entities.
type ProductPropertyVenueSelect struct {
	*ProductPropertyVenueQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ppvs *ProductPropertyVenueSelect) Aggregate(fns ...AggregateFunc) *ProductPropertyVenueSelect {
	ppvs.fns = append(ppvs.fns, fns...)
	return ppvs
}

// Scan applies the selector query and scans the result into the given value.
func (ppvs *ProductPropertyVenueSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ppvs.ctx, "Select")
	if err := ppvs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProductPropertyVenueQuery, *ProductPropertyVenueSelect](ctx, ppvs.ProductPropertyVenueQuery, ppvs, ppvs.inters, v)
}

func (ppvs *ProductPropertyVenueSelect) sqlScan(ctx context.Context, root *ProductPropertyVenueQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ppvs.fns))
	for _, fn := range ppvs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ppvs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ppvs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
