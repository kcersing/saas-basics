// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"saas/biz/dal/db/ent/predicate"
	"saas/biz/dal/db/ent/user"
	"saas/biz/dal/db/ent/usertimeperiod"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UserTimePeriodQuery is the builder for querying UserTimePeriod entities.
type UserTimePeriodQuery struct {
	config
	ctx        *QueryContext
	order      []usertimeperiod.OrderOption
	inters     []Interceptor
	predicates []predicate.UserTimePeriod
	withUsers  *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserTimePeriodQuery builder.
func (utpq *UserTimePeriodQuery) Where(ps ...predicate.UserTimePeriod) *UserTimePeriodQuery {
	utpq.predicates = append(utpq.predicates, ps...)
	return utpq
}

// Limit the number of records to be returned by this query.
func (utpq *UserTimePeriodQuery) Limit(limit int) *UserTimePeriodQuery {
	utpq.ctx.Limit = &limit
	return utpq
}

// Offset to start from.
func (utpq *UserTimePeriodQuery) Offset(offset int) *UserTimePeriodQuery {
	utpq.ctx.Offset = &offset
	return utpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (utpq *UserTimePeriodQuery) Unique(unique bool) *UserTimePeriodQuery {
	utpq.ctx.Unique = &unique
	return utpq
}

// Order specifies how the records should be ordered.
func (utpq *UserTimePeriodQuery) Order(o ...usertimeperiod.OrderOption) *UserTimePeriodQuery {
	utpq.order = append(utpq.order, o...)
	return utpq
}

// QueryUsers chains the current query on the "users" edge.
func (utpq *UserTimePeriodQuery) QueryUsers() *UserQuery {
	query := (&UserClient{config: utpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := utpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := utpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(usertimeperiod.Table, usertimeperiod.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, usertimeperiod.UsersTable, usertimeperiod.UsersColumn),
		)
		fromU = sqlgraph.SetNeighbors(utpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UserTimePeriod entity from the query.
// Returns a *NotFoundError when no UserTimePeriod was found.
func (utpq *UserTimePeriodQuery) First(ctx context.Context) (*UserTimePeriod, error) {
	nodes, err := utpq.Limit(1).All(setContextOp(ctx, utpq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{usertimeperiod.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (utpq *UserTimePeriodQuery) FirstX(ctx context.Context) *UserTimePeriod {
	node, err := utpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserTimePeriod ID from the query.
// Returns a *NotFoundError when no UserTimePeriod ID was found.
func (utpq *UserTimePeriodQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = utpq.Limit(1).IDs(setContextOp(ctx, utpq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{usertimeperiod.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (utpq *UserTimePeriodQuery) FirstIDX(ctx context.Context) int64 {
	id, err := utpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserTimePeriod entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserTimePeriod entity is found.
// Returns a *NotFoundError when no UserTimePeriod entities are found.
func (utpq *UserTimePeriodQuery) Only(ctx context.Context) (*UserTimePeriod, error) {
	nodes, err := utpq.Limit(2).All(setContextOp(ctx, utpq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{usertimeperiod.Label}
	default:
		return nil, &NotSingularError{usertimeperiod.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (utpq *UserTimePeriodQuery) OnlyX(ctx context.Context) *UserTimePeriod {
	node, err := utpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserTimePeriod ID in the query.
// Returns a *NotSingularError when more than one UserTimePeriod ID is found.
// Returns a *NotFoundError when no entities are found.
func (utpq *UserTimePeriodQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = utpq.Limit(2).IDs(setContextOp(ctx, utpq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{usertimeperiod.Label}
	default:
		err = &NotSingularError{usertimeperiod.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (utpq *UserTimePeriodQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := utpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserTimePeriods.
func (utpq *UserTimePeriodQuery) All(ctx context.Context) ([]*UserTimePeriod, error) {
	ctx = setContextOp(ctx, utpq.ctx, ent.OpQueryAll)
	if err := utpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserTimePeriod, *UserTimePeriodQuery]()
	return withInterceptors[[]*UserTimePeriod](ctx, utpq, qr, utpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (utpq *UserTimePeriodQuery) AllX(ctx context.Context) []*UserTimePeriod {
	nodes, err := utpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserTimePeriod IDs.
func (utpq *UserTimePeriodQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if utpq.ctx.Unique == nil && utpq.path != nil {
		utpq.Unique(true)
	}
	ctx = setContextOp(ctx, utpq.ctx, ent.OpQueryIDs)
	if err = utpq.Select(usertimeperiod.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (utpq *UserTimePeriodQuery) IDsX(ctx context.Context) []int64 {
	ids, err := utpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (utpq *UserTimePeriodQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, utpq.ctx, ent.OpQueryCount)
	if err := utpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, utpq, querierCount[*UserTimePeriodQuery](), utpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (utpq *UserTimePeriodQuery) CountX(ctx context.Context) int {
	count, err := utpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (utpq *UserTimePeriodQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, utpq.ctx, ent.OpQueryExist)
	switch _, err := utpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (utpq *UserTimePeriodQuery) ExistX(ctx context.Context) bool {
	exist, err := utpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserTimePeriodQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (utpq *UserTimePeriodQuery) Clone() *UserTimePeriodQuery {
	if utpq == nil {
		return nil
	}
	return &UserTimePeriodQuery{
		config:     utpq.config,
		ctx:        utpq.ctx.Clone(),
		order:      append([]usertimeperiod.OrderOption{}, utpq.order...),
		inters:     append([]Interceptor{}, utpq.inters...),
		predicates: append([]predicate.UserTimePeriod{}, utpq.predicates...),
		withUsers:  utpq.withUsers.Clone(),
		// clone intermediate query.
		sql:  utpq.sql.Clone(),
		path: utpq.path,
	}
}

// WithUsers tells the query-builder to eager-load the nodes that are connected to
// the "users" edge. The optional arguments are used to configure the query builder of the edge.
func (utpq *UserTimePeriodQuery) WithUsers(opts ...func(*UserQuery)) *UserTimePeriodQuery {
	query := (&UserClient{config: utpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	utpq.withUsers = query
	return utpq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserTimePeriod.Query().
//		GroupBy(usertimeperiod.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (utpq *UserTimePeriodQuery) GroupBy(field string, fields ...string) *UserTimePeriodGroupBy {
	utpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserTimePeriodGroupBy{build: utpq}
	grbuild.flds = &utpq.ctx.Fields
	grbuild.label = usertimeperiod.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.UserTimePeriod.Query().
//		Select(usertimeperiod.FieldCreatedAt).
//		Scan(ctx, &v)
func (utpq *UserTimePeriodQuery) Select(fields ...string) *UserTimePeriodSelect {
	utpq.ctx.Fields = append(utpq.ctx.Fields, fields...)
	sbuild := &UserTimePeriodSelect{UserTimePeriodQuery: utpq}
	sbuild.label = usertimeperiod.Label
	sbuild.flds, sbuild.scan = &utpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserTimePeriodSelect configured with the given aggregations.
func (utpq *UserTimePeriodQuery) Aggregate(fns ...AggregateFunc) *UserTimePeriodSelect {
	return utpq.Select().Aggregate(fns...)
}

func (utpq *UserTimePeriodQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range utpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, utpq); err != nil {
				return err
			}
		}
	}
	for _, f := range utpq.ctx.Fields {
		if !usertimeperiod.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if utpq.path != nil {
		prev, err := utpq.path(ctx)
		if err != nil {
			return err
		}
		utpq.sql = prev
	}
	return nil
}

func (utpq *UserTimePeriodQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserTimePeriod, error) {
	var (
		nodes       = []*UserTimePeriod{}
		_spec       = utpq.querySpec()
		loadedTypes = [1]bool{
			utpq.withUsers != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserTimePeriod).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserTimePeriod{config: utpq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, utpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := utpq.withUsers; query != nil {
		if err := utpq.loadUsers(ctx, query, nodes, nil,
			func(n *UserTimePeriod, e *User) { n.Edges.Users = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (utpq *UserTimePeriodQuery) loadUsers(ctx context.Context, query *UserQuery, nodes []*UserTimePeriod, init func(*UserTimePeriod), assign func(*UserTimePeriod, *User)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*UserTimePeriod)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (utpq *UserTimePeriodQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := utpq.querySpec()
	_spec.Node.Columns = utpq.ctx.Fields
	if len(utpq.ctx.Fields) > 0 {
		_spec.Unique = utpq.ctx.Unique != nil && *utpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, utpq.driver, _spec)
}

func (utpq *UserTimePeriodQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(usertimeperiod.Table, usertimeperiod.Columns, sqlgraph.NewFieldSpec(usertimeperiod.FieldID, field.TypeInt64))
	_spec.From = utpq.sql
	if unique := utpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if utpq.path != nil {
		_spec.Unique = true
	}
	if fields := utpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, usertimeperiod.FieldID)
		for i := range fields {
			if fields[i] != usertimeperiod.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if utpq.withUsers != nil {
			_spec.Node.AddColumnOnce(usertimeperiod.FieldUserID)
		}
	}
	if ps := utpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := utpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := utpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := utpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (utpq *UserTimePeriodQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(utpq.driver.Dialect())
	t1 := builder.Table(usertimeperiod.Table)
	columns := utpq.ctx.Fields
	if len(columns) == 0 {
		columns = usertimeperiod.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if utpq.sql != nil {
		selector = utpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if utpq.ctx.Unique != nil && *utpq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range utpq.predicates {
		p(selector)
	}
	for _, p := range utpq.order {
		p(selector)
	}
	if offset := utpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := utpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserTimePeriodGroupBy is the group-by builder for UserTimePeriod entities.
type UserTimePeriodGroupBy struct {
	selector
	build *UserTimePeriodQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (utpgb *UserTimePeriodGroupBy) Aggregate(fns ...AggregateFunc) *UserTimePeriodGroupBy {
	utpgb.fns = append(utpgb.fns, fns...)
	return utpgb
}

// Scan applies the selector query and scans the result into the given value.
func (utpgb *UserTimePeriodGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, utpgb.build.ctx, ent.OpQueryGroupBy)
	if err := utpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserTimePeriodQuery, *UserTimePeriodGroupBy](ctx, utpgb.build, utpgb, utpgb.build.inters, v)
}

func (utpgb *UserTimePeriodGroupBy) sqlScan(ctx context.Context, root *UserTimePeriodQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(utpgb.fns))
	for _, fn := range utpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*utpgb.flds)+len(utpgb.fns))
		for _, f := range *utpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*utpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := utpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserTimePeriodSelect is the builder for selecting fields of UserTimePeriod entities.
type UserTimePeriodSelect struct {
	*UserTimePeriodQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (utps *UserTimePeriodSelect) Aggregate(fns ...AggregateFunc) *UserTimePeriodSelect {
	utps.fns = append(utps.fns, fns...)
	return utps
}

// Scan applies the selector query and scans the result into the given value.
func (utps *UserTimePeriodSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, utps.ctx, ent.OpQuerySelect)
	if err := utps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserTimePeriodQuery, *UserTimePeriodSelect](ctx, utps.UserTimePeriodQuery, utps, utps.inters, v)
}

func (utps *UserTimePeriodSelect) sqlScan(ctx context.Context, root *UserTimePeriodQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(utps.fns))
	for _, fn := range utps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*utps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := utps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
