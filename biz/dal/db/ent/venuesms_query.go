// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"saas/biz/dal/db/ent/predicate"
	"saas/biz/dal/db/ent/venue"
	"saas/biz/dal/db/ent/venuesms"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// VenueSmsQuery is the builder for querying VenueSms entities.
type VenueSmsQuery struct {
	config
	ctx        *QueryContext
	order      []venuesms.OrderOption
	inters     []Interceptor
	predicates []predicate.VenueSms
	withVenue  *VenueQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the VenueSmsQuery builder.
func (vsq *VenueSmsQuery) Where(ps ...predicate.VenueSms) *VenueSmsQuery {
	vsq.predicates = append(vsq.predicates, ps...)
	return vsq
}

// Limit the number of records to be returned by this query.
func (vsq *VenueSmsQuery) Limit(limit int) *VenueSmsQuery {
	vsq.ctx.Limit = &limit
	return vsq
}

// Offset to start from.
func (vsq *VenueSmsQuery) Offset(offset int) *VenueSmsQuery {
	vsq.ctx.Offset = &offset
	return vsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (vsq *VenueSmsQuery) Unique(unique bool) *VenueSmsQuery {
	vsq.ctx.Unique = &unique
	return vsq
}

// Order specifies how the records should be ordered.
func (vsq *VenueSmsQuery) Order(o ...venuesms.OrderOption) *VenueSmsQuery {
	vsq.order = append(vsq.order, o...)
	return vsq
}

// QueryVenue chains the current query on the "venue" edge.
func (vsq *VenueSmsQuery) QueryVenue() *VenueQuery {
	query := (&VenueClient{config: vsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := vsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := vsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(venuesms.Table, venuesms.FieldID, selector),
			sqlgraph.To(venue.Table, venue.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, venuesms.VenueTable, venuesms.VenueColumn),
		)
		fromU = sqlgraph.SetNeighbors(vsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first VenueSms entity from the query.
// Returns a *NotFoundError when no VenueSms was found.
func (vsq *VenueSmsQuery) First(ctx context.Context) (*VenueSms, error) {
	nodes, err := vsq.Limit(1).All(setContextOp(ctx, vsq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{venuesms.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (vsq *VenueSmsQuery) FirstX(ctx context.Context) *VenueSms {
	node, err := vsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first VenueSms ID from the query.
// Returns a *NotFoundError when no VenueSms ID was found.
func (vsq *VenueSmsQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = vsq.Limit(1).IDs(setContextOp(ctx, vsq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{venuesms.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (vsq *VenueSmsQuery) FirstIDX(ctx context.Context) int64 {
	id, err := vsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single VenueSms entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one VenueSms entity is found.
// Returns a *NotFoundError when no VenueSms entities are found.
func (vsq *VenueSmsQuery) Only(ctx context.Context) (*VenueSms, error) {
	nodes, err := vsq.Limit(2).All(setContextOp(ctx, vsq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{venuesms.Label}
	default:
		return nil, &NotSingularError{venuesms.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (vsq *VenueSmsQuery) OnlyX(ctx context.Context) *VenueSms {
	node, err := vsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only VenueSms ID in the query.
// Returns a *NotSingularError when more than one VenueSms ID is found.
// Returns a *NotFoundError when no entities are found.
func (vsq *VenueSmsQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = vsq.Limit(2).IDs(setContextOp(ctx, vsq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{venuesms.Label}
	default:
		err = &NotSingularError{venuesms.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (vsq *VenueSmsQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := vsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of VenueSmsSlice.
func (vsq *VenueSmsQuery) All(ctx context.Context) ([]*VenueSms, error) {
	ctx = setContextOp(ctx, vsq.ctx, "All")
	if err := vsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*VenueSms, *VenueSmsQuery]()
	return withInterceptors[[]*VenueSms](ctx, vsq, qr, vsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (vsq *VenueSmsQuery) AllX(ctx context.Context) []*VenueSms {
	nodes, err := vsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of VenueSms IDs.
func (vsq *VenueSmsQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if vsq.ctx.Unique == nil && vsq.path != nil {
		vsq.Unique(true)
	}
	ctx = setContextOp(ctx, vsq.ctx, "IDs")
	if err = vsq.Select(venuesms.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (vsq *VenueSmsQuery) IDsX(ctx context.Context) []int64 {
	ids, err := vsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (vsq *VenueSmsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, vsq.ctx, "Count")
	if err := vsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, vsq, querierCount[*VenueSmsQuery](), vsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (vsq *VenueSmsQuery) CountX(ctx context.Context) int {
	count, err := vsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (vsq *VenueSmsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, vsq.ctx, "Exist")
	switch _, err := vsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (vsq *VenueSmsQuery) ExistX(ctx context.Context) bool {
	exist, err := vsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the VenueSmsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (vsq *VenueSmsQuery) Clone() *VenueSmsQuery {
	if vsq == nil {
		return nil
	}
	return &VenueSmsQuery{
		config:     vsq.config,
		ctx:        vsq.ctx.Clone(),
		order:      append([]venuesms.OrderOption{}, vsq.order...),
		inters:     append([]Interceptor{}, vsq.inters...),
		predicates: append([]predicate.VenueSms{}, vsq.predicates...),
		withVenue:  vsq.withVenue.Clone(),
		// clone intermediate query.
		sql:  vsq.sql.Clone(),
		path: vsq.path,
	}
}

// WithVenue tells the query-builder to eager-load the nodes that are connected to
// the "venue" edge. The optional arguments are used to configure the query builder of the edge.
func (vsq *VenueSmsQuery) WithVenue(opts ...func(*VenueQuery)) *VenueSmsQuery {
	query := (&VenueClient{config: vsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	vsq.withVenue = query
	return vsq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.VenueSms.Query().
//		GroupBy(venuesms.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (vsq *VenueSmsQuery) GroupBy(field string, fields ...string) *VenueSmsGroupBy {
	vsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &VenueSmsGroupBy{build: vsq}
	grbuild.flds = &vsq.ctx.Fields
	grbuild.label = venuesms.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.VenueSms.Query().
//		Select(venuesms.FieldCreatedAt).
//		Scan(ctx, &v)
func (vsq *VenueSmsQuery) Select(fields ...string) *VenueSmsSelect {
	vsq.ctx.Fields = append(vsq.ctx.Fields, fields...)
	sbuild := &VenueSmsSelect{VenueSmsQuery: vsq}
	sbuild.label = venuesms.Label
	sbuild.flds, sbuild.scan = &vsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a VenueSmsSelect configured with the given aggregations.
func (vsq *VenueSmsQuery) Aggregate(fns ...AggregateFunc) *VenueSmsSelect {
	return vsq.Select().Aggregate(fns...)
}

func (vsq *VenueSmsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range vsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, vsq); err != nil {
				return err
			}
		}
	}
	for _, f := range vsq.ctx.Fields {
		if !venuesms.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if vsq.path != nil {
		prev, err := vsq.path(ctx)
		if err != nil {
			return err
		}
		vsq.sql = prev
	}
	return nil
}

func (vsq *VenueSmsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*VenueSms, error) {
	var (
		nodes       = []*VenueSms{}
		_spec       = vsq.querySpec()
		loadedTypes = [1]bool{
			vsq.withVenue != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*VenueSms).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &VenueSms{config: vsq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, vsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := vsq.withVenue; query != nil {
		if err := vsq.loadVenue(ctx, query, nodes, nil,
			func(n *VenueSms, e *Venue) { n.Edges.Venue = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (vsq *VenueSmsQuery) loadVenue(ctx context.Context, query *VenueQuery, nodes []*VenueSms, init func(*VenueSms), assign func(*VenueSms, *Venue)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*VenueSms)
	for i := range nodes {
		fk := nodes[i].VenueID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(venue.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "venue_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (vsq *VenueSmsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := vsq.querySpec()
	_spec.Node.Columns = vsq.ctx.Fields
	if len(vsq.ctx.Fields) > 0 {
		_spec.Unique = vsq.ctx.Unique != nil && *vsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, vsq.driver, _spec)
}

func (vsq *VenueSmsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(venuesms.Table, venuesms.Columns, sqlgraph.NewFieldSpec(venuesms.FieldID, field.TypeInt64))
	_spec.From = vsq.sql
	if unique := vsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if vsq.path != nil {
		_spec.Unique = true
	}
	if fields := vsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, venuesms.FieldID)
		for i := range fields {
			if fields[i] != venuesms.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if vsq.withVenue != nil {
			_spec.Node.AddColumnOnce(venuesms.FieldVenueID)
		}
	}
	if ps := vsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := vsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := vsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := vsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (vsq *VenueSmsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(vsq.driver.Dialect())
	t1 := builder.Table(venuesms.Table)
	columns := vsq.ctx.Fields
	if len(columns) == 0 {
		columns = venuesms.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if vsq.sql != nil {
		selector = vsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if vsq.ctx.Unique != nil && *vsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range vsq.predicates {
		p(selector)
	}
	for _, p := range vsq.order {
		p(selector)
	}
	if offset := vsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := vsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// VenueSmsGroupBy is the group-by builder for VenueSms entities.
type VenueSmsGroupBy struct {
	selector
	build *VenueSmsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (vsgb *VenueSmsGroupBy) Aggregate(fns ...AggregateFunc) *VenueSmsGroupBy {
	vsgb.fns = append(vsgb.fns, fns...)
	return vsgb
}

// Scan applies the selector query and scans the result into the given value.
func (vsgb *VenueSmsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vsgb.build.ctx, "GroupBy")
	if err := vsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VenueSmsQuery, *VenueSmsGroupBy](ctx, vsgb.build, vsgb, vsgb.build.inters, v)
}

func (vsgb *VenueSmsGroupBy) sqlScan(ctx context.Context, root *VenueSmsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(vsgb.fns))
	for _, fn := range vsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*vsgb.flds)+len(vsgb.fns))
		for _, f := range *vsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*vsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// VenueSmsSelect is the builder for selecting fields of VenueSms entities.
type VenueSmsSelect struct {
	*VenueSmsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (vss *VenueSmsSelect) Aggregate(fns ...AggregateFunc) *VenueSmsSelect {
	vss.fns = append(vss.fns, fns...)
	return vss
}

// Scan applies the selector query and scans the result into the given value.
func (vss *VenueSmsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vss.ctx, "Select")
	if err := vss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VenueSmsQuery, *VenueSmsSelect](ctx, vss.VenueSmsQuery, vss, vss.inters, v)
}

func (vss *VenueSmsSelect) sqlScan(ctx context.Context, root *VenueSmsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(vss.fns))
	for _, fn := range vss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*vss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
