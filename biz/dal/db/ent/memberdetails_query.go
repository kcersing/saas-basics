// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"saas/biz/dal/db/ent/member"
	"saas/biz/dal/db/ent/memberdetails"
	"saas/biz/dal/db/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// MemberDetailsQuery is the builder for querying MemberDetails entities.
type MemberDetailsQuery struct {
	config
	ctx        *QueryContext
	order      []memberdetails.OrderOption
	inters     []Interceptor
	predicates []predicate.MemberDetails
	withMember *MemberQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MemberDetailsQuery builder.
func (mdq *MemberDetailsQuery) Where(ps ...predicate.MemberDetails) *MemberDetailsQuery {
	mdq.predicates = append(mdq.predicates, ps...)
	return mdq
}

// Limit the number of records to be returned by this query.
func (mdq *MemberDetailsQuery) Limit(limit int) *MemberDetailsQuery {
	mdq.ctx.Limit = &limit
	return mdq
}

// Offset to start from.
func (mdq *MemberDetailsQuery) Offset(offset int) *MemberDetailsQuery {
	mdq.ctx.Offset = &offset
	return mdq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mdq *MemberDetailsQuery) Unique(unique bool) *MemberDetailsQuery {
	mdq.ctx.Unique = &unique
	return mdq
}

// Order specifies how the records should be ordered.
func (mdq *MemberDetailsQuery) Order(o ...memberdetails.OrderOption) *MemberDetailsQuery {
	mdq.order = append(mdq.order, o...)
	return mdq
}

// QueryMember chains the current query on the "member" edge.
func (mdq *MemberDetailsQuery) QueryMember() *MemberQuery {
	query := (&MemberClient{config: mdq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mdq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mdq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(memberdetails.Table, memberdetails.FieldID, selector),
			sqlgraph.To(member.Table, member.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, memberdetails.MemberTable, memberdetails.MemberColumn),
		)
		fromU = sqlgraph.SetNeighbors(mdq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first MemberDetails entity from the query.
// Returns a *NotFoundError when no MemberDetails was found.
func (mdq *MemberDetailsQuery) First(ctx context.Context) (*MemberDetails, error) {
	nodes, err := mdq.Limit(1).All(setContextOp(ctx, mdq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{memberdetails.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mdq *MemberDetailsQuery) FirstX(ctx context.Context) *MemberDetails {
	node, err := mdq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MemberDetails ID from the query.
// Returns a *NotFoundError when no MemberDetails ID was found.
func (mdq *MemberDetailsQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = mdq.Limit(1).IDs(setContextOp(ctx, mdq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{memberdetails.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mdq *MemberDetailsQuery) FirstIDX(ctx context.Context) int64 {
	id, err := mdq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MemberDetails entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MemberDetails entity is found.
// Returns a *NotFoundError when no MemberDetails entities are found.
func (mdq *MemberDetailsQuery) Only(ctx context.Context) (*MemberDetails, error) {
	nodes, err := mdq.Limit(2).All(setContextOp(ctx, mdq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{memberdetails.Label}
	default:
		return nil, &NotSingularError{memberdetails.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mdq *MemberDetailsQuery) OnlyX(ctx context.Context) *MemberDetails {
	node, err := mdq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MemberDetails ID in the query.
// Returns a *NotSingularError when more than one MemberDetails ID is found.
// Returns a *NotFoundError when no entities are found.
func (mdq *MemberDetailsQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = mdq.Limit(2).IDs(setContextOp(ctx, mdq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{memberdetails.Label}
	default:
		err = &NotSingularError{memberdetails.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mdq *MemberDetailsQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := mdq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MemberDetailsSlice.
func (mdq *MemberDetailsQuery) All(ctx context.Context) ([]*MemberDetails, error) {
	ctx = setContextOp(ctx, mdq.ctx, "All")
	if err := mdq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MemberDetails, *MemberDetailsQuery]()
	return withInterceptors[[]*MemberDetails](ctx, mdq, qr, mdq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mdq *MemberDetailsQuery) AllX(ctx context.Context) []*MemberDetails {
	nodes, err := mdq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MemberDetails IDs.
func (mdq *MemberDetailsQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if mdq.ctx.Unique == nil && mdq.path != nil {
		mdq.Unique(true)
	}
	ctx = setContextOp(ctx, mdq.ctx, "IDs")
	if err = mdq.Select(memberdetails.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mdq *MemberDetailsQuery) IDsX(ctx context.Context) []int64 {
	ids, err := mdq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mdq *MemberDetailsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mdq.ctx, "Count")
	if err := mdq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mdq, querierCount[*MemberDetailsQuery](), mdq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mdq *MemberDetailsQuery) CountX(ctx context.Context) int {
	count, err := mdq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mdq *MemberDetailsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mdq.ctx, "Exist")
	switch _, err := mdq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mdq *MemberDetailsQuery) ExistX(ctx context.Context) bool {
	exist, err := mdq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MemberDetailsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mdq *MemberDetailsQuery) Clone() *MemberDetailsQuery {
	if mdq == nil {
		return nil
	}
	return &MemberDetailsQuery{
		config:     mdq.config,
		ctx:        mdq.ctx.Clone(),
		order:      append([]memberdetails.OrderOption{}, mdq.order...),
		inters:     append([]Interceptor{}, mdq.inters...),
		predicates: append([]predicate.MemberDetails{}, mdq.predicates...),
		withMember: mdq.withMember.Clone(),
		// clone intermediate query.
		sql:  mdq.sql.Clone(),
		path: mdq.path,
	}
}

// WithMember tells the query-builder to eager-load the nodes that are connected to
// the "member" edge. The optional arguments are used to configure the query builder of the edge.
func (mdq *MemberDetailsQuery) WithMember(opts ...func(*MemberQuery)) *MemberDetailsQuery {
	query := (&MemberClient{config: mdq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mdq.withMember = query
	return mdq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MemberDetails.Query().
//		GroupBy(memberdetails.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mdq *MemberDetailsQuery) GroupBy(field string, fields ...string) *MemberDetailsGroupBy {
	mdq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MemberDetailsGroupBy{build: mdq}
	grbuild.flds = &mdq.ctx.Fields
	grbuild.label = memberdetails.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.MemberDetails.Query().
//		Select(memberdetails.FieldCreatedAt).
//		Scan(ctx, &v)
func (mdq *MemberDetailsQuery) Select(fields ...string) *MemberDetailsSelect {
	mdq.ctx.Fields = append(mdq.ctx.Fields, fields...)
	sbuild := &MemberDetailsSelect{MemberDetailsQuery: mdq}
	sbuild.label = memberdetails.Label
	sbuild.flds, sbuild.scan = &mdq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MemberDetailsSelect configured with the given aggregations.
func (mdq *MemberDetailsQuery) Aggregate(fns ...AggregateFunc) *MemberDetailsSelect {
	return mdq.Select().Aggregate(fns...)
}

func (mdq *MemberDetailsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mdq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mdq); err != nil {
				return err
			}
		}
	}
	for _, f := range mdq.ctx.Fields {
		if !memberdetails.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mdq.path != nil {
		prev, err := mdq.path(ctx)
		if err != nil {
			return err
		}
		mdq.sql = prev
	}
	return nil
}

func (mdq *MemberDetailsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MemberDetails, error) {
	var (
		nodes       = []*MemberDetails{}
		_spec       = mdq.querySpec()
		loadedTypes = [1]bool{
			mdq.withMember != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MemberDetails).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MemberDetails{config: mdq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mdq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := mdq.withMember; query != nil {
		if err := mdq.loadMember(ctx, query, nodes, nil,
			func(n *MemberDetails, e *Member) { n.Edges.Member = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (mdq *MemberDetailsQuery) loadMember(ctx context.Context, query *MemberQuery, nodes []*MemberDetails, init func(*MemberDetails), assign func(*MemberDetails, *Member)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*MemberDetails)
	for i := range nodes {
		fk := nodes[i].MemberID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(member.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "member_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (mdq *MemberDetailsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mdq.querySpec()
	_spec.Node.Columns = mdq.ctx.Fields
	if len(mdq.ctx.Fields) > 0 {
		_spec.Unique = mdq.ctx.Unique != nil && *mdq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mdq.driver, _spec)
}

func (mdq *MemberDetailsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(memberdetails.Table, memberdetails.Columns, sqlgraph.NewFieldSpec(memberdetails.FieldID, field.TypeInt64))
	_spec.From = mdq.sql
	if unique := mdq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mdq.path != nil {
		_spec.Unique = true
	}
	if fields := mdq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, memberdetails.FieldID)
		for i := range fields {
			if fields[i] != memberdetails.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if mdq.withMember != nil {
			_spec.Node.AddColumnOnce(memberdetails.FieldMemberID)
		}
	}
	if ps := mdq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mdq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mdq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mdq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mdq *MemberDetailsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mdq.driver.Dialect())
	t1 := builder.Table(memberdetails.Table)
	columns := mdq.ctx.Fields
	if len(columns) == 0 {
		columns = memberdetails.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mdq.sql != nil {
		selector = mdq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mdq.ctx.Unique != nil && *mdq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range mdq.predicates {
		p(selector)
	}
	for _, p := range mdq.order {
		p(selector)
	}
	if offset := mdq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mdq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MemberDetailsGroupBy is the group-by builder for MemberDetails entities.
type MemberDetailsGroupBy struct {
	selector
	build *MemberDetailsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mdgb *MemberDetailsGroupBy) Aggregate(fns ...AggregateFunc) *MemberDetailsGroupBy {
	mdgb.fns = append(mdgb.fns, fns...)
	return mdgb
}

// Scan applies the selector query and scans the result into the given value.
func (mdgb *MemberDetailsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mdgb.build.ctx, "GroupBy")
	if err := mdgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MemberDetailsQuery, *MemberDetailsGroupBy](ctx, mdgb.build, mdgb, mdgb.build.inters, v)
}

func (mdgb *MemberDetailsGroupBy) sqlScan(ctx context.Context, root *MemberDetailsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mdgb.fns))
	for _, fn := range mdgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mdgb.flds)+len(mdgb.fns))
		for _, f := range *mdgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mdgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mdgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MemberDetailsSelect is the builder for selecting fields of MemberDetails entities.
type MemberDetailsSelect struct {
	*MemberDetailsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mds *MemberDetailsSelect) Aggregate(fns ...AggregateFunc) *MemberDetailsSelect {
	mds.fns = append(mds.fns, fns...)
	return mds
}

// Scan applies the selector query and scans the result into the given value.
func (mds *MemberDetailsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mds.ctx, "Select")
	if err := mds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MemberDetailsQuery, *MemberDetailsSelect](ctx, mds.MemberDetailsQuery, mds, mds.inters, v)
}

func (mds *MemberDetailsSelect) sqlScan(ctx context.Context, root *MemberDetailsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mds.fns))
	for _, fn := range mds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
