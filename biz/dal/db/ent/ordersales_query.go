// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"saas/biz/dal/db/ent/order"
	"saas/biz/dal/db/ent/ordersales"
	"saas/biz/dal/db/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// OrderSalesQuery is the builder for querying OrderSales entities.
type OrderSalesQuery struct {
	config
	ctx        *QueryContext
	order      []ordersales.OrderOption
	inters     []Interceptor
	predicates []predicate.OrderSales
	withOrder  *OrderQuery
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OrderSalesQuery builder.
func (osq *OrderSalesQuery) Where(ps ...predicate.OrderSales) *OrderSalesQuery {
	osq.predicates = append(osq.predicates, ps...)
	return osq
}

// Limit the number of records to be returned by this query.
func (osq *OrderSalesQuery) Limit(limit int) *OrderSalesQuery {
	osq.ctx.Limit = &limit
	return osq
}

// Offset to start from.
func (osq *OrderSalesQuery) Offset(offset int) *OrderSalesQuery {
	osq.ctx.Offset = &offset
	return osq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (osq *OrderSalesQuery) Unique(unique bool) *OrderSalesQuery {
	osq.ctx.Unique = &unique
	return osq
}

// Order specifies how the records should be ordered.
func (osq *OrderSalesQuery) Order(o ...ordersales.OrderOption) *OrderSalesQuery {
	osq.order = append(osq.order, o...)
	return osq
}

// QueryOrder chains the current query on the "order" edge.
func (osq *OrderSalesQuery) QueryOrder() *OrderQuery {
	query := (&OrderClient{config: osq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := osq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := osq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(ordersales.Table, ordersales.FieldID, selector),
			sqlgraph.To(order.Table, order.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, ordersales.OrderTable, ordersales.OrderColumn),
		)
		fromU = sqlgraph.SetNeighbors(osq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first OrderSales entity from the query.
// Returns a *NotFoundError when no OrderSales was found.
func (osq *OrderSalesQuery) First(ctx context.Context) (*OrderSales, error) {
	nodes, err := osq.Limit(1).All(setContextOp(ctx, osq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{ordersales.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (osq *OrderSalesQuery) FirstX(ctx context.Context) *OrderSales {
	node, err := osq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OrderSales ID from the query.
// Returns a *NotFoundError when no OrderSales ID was found.
func (osq *OrderSalesQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = osq.Limit(1).IDs(setContextOp(ctx, osq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{ordersales.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (osq *OrderSalesQuery) FirstIDX(ctx context.Context) int64 {
	id, err := osq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OrderSales entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OrderSales entity is found.
// Returns a *NotFoundError when no OrderSales entities are found.
func (osq *OrderSalesQuery) Only(ctx context.Context) (*OrderSales, error) {
	nodes, err := osq.Limit(2).All(setContextOp(ctx, osq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{ordersales.Label}
	default:
		return nil, &NotSingularError{ordersales.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (osq *OrderSalesQuery) OnlyX(ctx context.Context) *OrderSales {
	node, err := osq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OrderSales ID in the query.
// Returns a *NotSingularError when more than one OrderSales ID is found.
// Returns a *NotFoundError when no entities are found.
func (osq *OrderSalesQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = osq.Limit(2).IDs(setContextOp(ctx, osq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{ordersales.Label}
	default:
		err = &NotSingularError{ordersales.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (osq *OrderSalesQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := osq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OrderSalesSlice.
func (osq *OrderSalesQuery) All(ctx context.Context) ([]*OrderSales, error) {
	ctx = setContextOp(ctx, osq.ctx, "All")
	if err := osq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OrderSales, *OrderSalesQuery]()
	return withInterceptors[[]*OrderSales](ctx, osq, qr, osq.inters)
}

// AllX is like All, but panics if an error occurs.
func (osq *OrderSalesQuery) AllX(ctx context.Context) []*OrderSales {
	nodes, err := osq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OrderSales IDs.
func (osq *OrderSalesQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if osq.ctx.Unique == nil && osq.path != nil {
		osq.Unique(true)
	}
	ctx = setContextOp(ctx, osq.ctx, "IDs")
	if err = osq.Select(ordersales.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (osq *OrderSalesQuery) IDsX(ctx context.Context) []int64 {
	ids, err := osq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (osq *OrderSalesQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, osq.ctx, "Count")
	if err := osq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, osq, querierCount[*OrderSalesQuery](), osq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (osq *OrderSalesQuery) CountX(ctx context.Context) int {
	count, err := osq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (osq *OrderSalesQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, osq.ctx, "Exist")
	switch _, err := osq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (osq *OrderSalesQuery) ExistX(ctx context.Context) bool {
	exist, err := osq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OrderSalesQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (osq *OrderSalesQuery) Clone() *OrderSalesQuery {
	if osq == nil {
		return nil
	}
	return &OrderSalesQuery{
		config:     osq.config,
		ctx:        osq.ctx.Clone(),
		order:      append([]ordersales.OrderOption{}, osq.order...),
		inters:     append([]Interceptor{}, osq.inters...),
		predicates: append([]predicate.OrderSales{}, osq.predicates...),
		withOrder:  osq.withOrder.Clone(),
		// clone intermediate query.
		sql:  osq.sql.Clone(),
		path: osq.path,
	}
}

// WithOrder tells the query-builder to eager-load the nodes that are connected to
// the "order" edge. The optional arguments are used to configure the query builder of the edge.
func (osq *OrderSalesQuery) WithOrder(opts ...func(*OrderQuery)) *OrderSalesQuery {
	query := (&OrderClient{config: osq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	osq.withOrder = query
	return osq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OrderSales.Query().
//		GroupBy(ordersales.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (osq *OrderSalesQuery) GroupBy(field string, fields ...string) *OrderSalesGroupBy {
	osq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OrderSalesGroupBy{build: osq}
	grbuild.flds = &osq.ctx.Fields
	grbuild.label = ordersales.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.OrderSales.Query().
//		Select(ordersales.FieldCreatedAt).
//		Scan(ctx, &v)
func (osq *OrderSalesQuery) Select(fields ...string) *OrderSalesSelect {
	osq.ctx.Fields = append(osq.ctx.Fields, fields...)
	sbuild := &OrderSalesSelect{OrderSalesQuery: osq}
	sbuild.label = ordersales.Label
	sbuild.flds, sbuild.scan = &osq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OrderSalesSelect configured with the given aggregations.
func (osq *OrderSalesQuery) Aggregate(fns ...AggregateFunc) *OrderSalesSelect {
	return osq.Select().Aggregate(fns...)
}

func (osq *OrderSalesQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range osq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, osq); err != nil {
				return err
			}
		}
	}
	for _, f := range osq.ctx.Fields {
		if !ordersales.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if osq.path != nil {
		prev, err := osq.path(ctx)
		if err != nil {
			return err
		}
		osq.sql = prev
	}
	return nil
}

func (osq *OrderSalesQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OrderSales, error) {
	var (
		nodes       = []*OrderSales{}
		_spec       = osq.querySpec()
		loadedTypes = [1]bool{
			osq.withOrder != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OrderSales).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OrderSales{config: osq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(osq.modifiers) > 0 {
		_spec.Modifiers = osq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, osq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := osq.withOrder; query != nil {
		if err := osq.loadOrder(ctx, query, nodes, nil,
			func(n *OrderSales, e *Order) { n.Edges.Order = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (osq *OrderSalesQuery) loadOrder(ctx context.Context, query *OrderQuery, nodes []*OrderSales, init func(*OrderSales), assign func(*OrderSales, *Order)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*OrderSales)
	for i := range nodes {
		fk := nodes[i].OrderID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(order.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "order_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (osq *OrderSalesQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := osq.querySpec()
	if len(osq.modifiers) > 0 {
		_spec.Modifiers = osq.modifiers
	}
	_spec.Node.Columns = osq.ctx.Fields
	if len(osq.ctx.Fields) > 0 {
		_spec.Unique = osq.ctx.Unique != nil && *osq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, osq.driver, _spec)
}

func (osq *OrderSalesQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(ordersales.Table, ordersales.Columns, sqlgraph.NewFieldSpec(ordersales.FieldID, field.TypeInt64))
	_spec.From = osq.sql
	if unique := osq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if osq.path != nil {
		_spec.Unique = true
	}
	if fields := osq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, ordersales.FieldID)
		for i := range fields {
			if fields[i] != ordersales.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if osq.withOrder != nil {
			_spec.Node.AddColumnOnce(ordersales.FieldOrderID)
		}
	}
	if ps := osq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := osq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := osq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := osq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (osq *OrderSalesQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(osq.driver.Dialect())
	t1 := builder.Table(ordersales.Table)
	columns := osq.ctx.Fields
	if len(columns) == 0 {
		columns = ordersales.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if osq.sql != nil {
		selector = osq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if osq.ctx.Unique != nil && *osq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range osq.modifiers {
		m(selector)
	}
	for _, p := range osq.predicates {
		p(selector)
	}
	for _, p := range osq.order {
		p(selector)
	}
	if offset := osq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := osq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (osq *OrderSalesQuery) Modify(modifiers ...func(s *sql.Selector)) *OrderSalesSelect {
	osq.modifiers = append(osq.modifiers, modifiers...)
	return osq.Select()
}

// OrderSalesGroupBy is the group-by builder for OrderSales entities.
type OrderSalesGroupBy struct {
	selector
	build *OrderSalesQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (osgb *OrderSalesGroupBy) Aggregate(fns ...AggregateFunc) *OrderSalesGroupBy {
	osgb.fns = append(osgb.fns, fns...)
	return osgb
}

// Scan applies the selector query and scans the result into the given value.
func (osgb *OrderSalesGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, osgb.build.ctx, "GroupBy")
	if err := osgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OrderSalesQuery, *OrderSalesGroupBy](ctx, osgb.build, osgb, osgb.build.inters, v)
}

func (osgb *OrderSalesGroupBy) sqlScan(ctx context.Context, root *OrderSalesQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(osgb.fns))
	for _, fn := range osgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*osgb.flds)+len(osgb.fns))
		for _, f := range *osgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*osgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := osgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OrderSalesSelect is the builder for selecting fields of OrderSales entities.
type OrderSalesSelect struct {
	*OrderSalesQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (oss *OrderSalesSelect) Aggregate(fns ...AggregateFunc) *OrderSalesSelect {
	oss.fns = append(oss.fns, fns...)
	return oss
}

// Scan applies the selector query and scans the result into the given value.
func (oss *OrderSalesSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, oss.ctx, "Select")
	if err := oss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OrderSalesQuery, *OrderSalesSelect](ctx, oss.OrderSalesQuery, oss, oss.inters, v)
}

func (oss *OrderSalesSelect) sqlScan(ctx context.Context, root *OrderSalesQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(oss.fns))
	for _, fn := range oss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*oss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := oss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (oss *OrderSalesSelect) Modify(modifiers ...func(s *sql.Selector)) *OrderSalesSelect {
	oss.modifiers = append(oss.modifiers, modifiers...)
	return oss
}
