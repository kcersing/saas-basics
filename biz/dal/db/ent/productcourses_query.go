// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"saas/biz/dal/db/ent/predicate"
	"saas/biz/dal/db/ent/product"
	"saas/biz/dal/db/ent/productcourses"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ProductCoursesQuery is the builder for querying ProductCourses entities.
type ProductCoursesQuery struct {
	config
	ctx        *QueryContext
	order      []productcourses.OrderOption
	inters     []Interceptor
	predicates []predicate.ProductCourses
	withNodeC  *ProductQuery
	withNodeL  *ProductQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProductCoursesQuery builder.
func (pcq *ProductCoursesQuery) Where(ps ...predicate.ProductCourses) *ProductCoursesQuery {
	pcq.predicates = append(pcq.predicates, ps...)
	return pcq
}

// Limit the number of records to be returned by this query.
func (pcq *ProductCoursesQuery) Limit(limit int) *ProductCoursesQuery {
	pcq.ctx.Limit = &limit
	return pcq
}

// Offset to start from.
func (pcq *ProductCoursesQuery) Offset(offset int) *ProductCoursesQuery {
	pcq.ctx.Offset = &offset
	return pcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pcq *ProductCoursesQuery) Unique(unique bool) *ProductCoursesQuery {
	pcq.ctx.Unique = &unique
	return pcq
}

// Order specifies how the records should be ordered.
func (pcq *ProductCoursesQuery) Order(o ...productcourses.OrderOption) *ProductCoursesQuery {
	pcq.order = append(pcq.order, o...)
	return pcq
}

// QueryNodeC chains the current query on the "nodeC" edge.
func (pcq *ProductCoursesQuery) QueryNodeC() *ProductQuery {
	query := (&ProductClient{config: pcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(productcourses.Table, productcourses.FieldID, selector),
			sqlgraph.To(product.Table, product.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, productcourses.NodeCTable, productcourses.NodeCColumn),
		)
		fromU = sqlgraph.SetNeighbors(pcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNodeL chains the current query on the "nodeL" edge.
func (pcq *ProductCoursesQuery) QueryNodeL() *ProductQuery {
	query := (&ProductClient{config: pcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(productcourses.Table, productcourses.FieldID, selector),
			sqlgraph.To(product.Table, product.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, productcourses.NodeLTable, productcourses.NodeLColumn),
		)
		fromU = sqlgraph.SetNeighbors(pcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ProductCourses entity from the query.
// Returns a *NotFoundError when no ProductCourses was found.
func (pcq *ProductCoursesQuery) First(ctx context.Context) (*ProductCourses, error) {
	nodes, err := pcq.Limit(1).All(setContextOp(ctx, pcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{productcourses.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pcq *ProductCoursesQuery) FirstX(ctx context.Context) *ProductCourses {
	node, err := pcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProductCourses ID from the query.
// Returns a *NotFoundError when no ProductCourses ID was found.
func (pcq *ProductCoursesQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = pcq.Limit(1).IDs(setContextOp(ctx, pcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{productcourses.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pcq *ProductCoursesQuery) FirstIDX(ctx context.Context) int64 {
	id, err := pcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProductCourses entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProductCourses entity is found.
// Returns a *NotFoundError when no ProductCourses entities are found.
func (pcq *ProductCoursesQuery) Only(ctx context.Context) (*ProductCourses, error) {
	nodes, err := pcq.Limit(2).All(setContextOp(ctx, pcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{productcourses.Label}
	default:
		return nil, &NotSingularError{productcourses.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pcq *ProductCoursesQuery) OnlyX(ctx context.Context) *ProductCourses {
	node, err := pcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProductCourses ID in the query.
// Returns a *NotSingularError when more than one ProductCourses ID is found.
// Returns a *NotFoundError when no entities are found.
func (pcq *ProductCoursesQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = pcq.Limit(2).IDs(setContextOp(ctx, pcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{productcourses.Label}
	default:
		err = &NotSingularError{productcourses.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pcq *ProductCoursesQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := pcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProductCoursesSlice.
func (pcq *ProductCoursesQuery) All(ctx context.Context) ([]*ProductCourses, error) {
	ctx = setContextOp(ctx, pcq.ctx, ent.OpQueryAll)
	if err := pcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProductCourses, *ProductCoursesQuery]()
	return withInterceptors[[]*ProductCourses](ctx, pcq, qr, pcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pcq *ProductCoursesQuery) AllX(ctx context.Context) []*ProductCourses {
	nodes, err := pcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProductCourses IDs.
func (pcq *ProductCoursesQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if pcq.ctx.Unique == nil && pcq.path != nil {
		pcq.Unique(true)
	}
	ctx = setContextOp(ctx, pcq.ctx, ent.OpQueryIDs)
	if err = pcq.Select(productcourses.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pcq *ProductCoursesQuery) IDsX(ctx context.Context) []int64 {
	ids, err := pcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pcq *ProductCoursesQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pcq.ctx, ent.OpQueryCount)
	if err := pcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pcq, querierCount[*ProductCoursesQuery](), pcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pcq *ProductCoursesQuery) CountX(ctx context.Context) int {
	count, err := pcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pcq *ProductCoursesQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pcq.ctx, ent.OpQueryExist)
	switch _, err := pcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pcq *ProductCoursesQuery) ExistX(ctx context.Context) bool {
	exist, err := pcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProductCoursesQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pcq *ProductCoursesQuery) Clone() *ProductCoursesQuery {
	if pcq == nil {
		return nil
	}
	return &ProductCoursesQuery{
		config:     pcq.config,
		ctx:        pcq.ctx.Clone(),
		order:      append([]productcourses.OrderOption{}, pcq.order...),
		inters:     append([]Interceptor{}, pcq.inters...),
		predicates: append([]predicate.ProductCourses{}, pcq.predicates...),
		withNodeC:  pcq.withNodeC.Clone(),
		withNodeL:  pcq.withNodeL.Clone(),
		// clone intermediate query.
		sql:  pcq.sql.Clone(),
		path: pcq.path,
	}
}

// WithNodeC tells the query-builder to eager-load the nodes that are connected to
// the "nodeC" edge. The optional arguments are used to configure the query builder of the edge.
func (pcq *ProductCoursesQuery) WithNodeC(opts ...func(*ProductQuery)) *ProductCoursesQuery {
	query := (&ProductClient{config: pcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pcq.withNodeC = query
	return pcq
}

// WithNodeL tells the query-builder to eager-load the nodes that are connected to
// the "nodeL" edge. The optional arguments are used to configure the query builder of the edge.
func (pcq *ProductCoursesQuery) WithNodeL(opts ...func(*ProductQuery)) *ProductCoursesQuery {
	query := (&ProductClient{config: pcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pcq.withNodeL = query
	return pcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProductCourses.Query().
//		GroupBy(productcourses.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pcq *ProductCoursesQuery) GroupBy(field string, fields ...string) *ProductCoursesGroupBy {
	pcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProductCoursesGroupBy{build: pcq}
	grbuild.flds = &pcq.ctx.Fields
	grbuild.label = productcourses.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ProductCourses.Query().
//		Select(productcourses.FieldCreatedAt).
//		Scan(ctx, &v)
func (pcq *ProductCoursesQuery) Select(fields ...string) *ProductCoursesSelect {
	pcq.ctx.Fields = append(pcq.ctx.Fields, fields...)
	sbuild := &ProductCoursesSelect{ProductCoursesQuery: pcq}
	sbuild.label = productcourses.Label
	sbuild.flds, sbuild.scan = &pcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProductCoursesSelect configured with the given aggregations.
func (pcq *ProductCoursesQuery) Aggregate(fns ...AggregateFunc) *ProductCoursesSelect {
	return pcq.Select().Aggregate(fns...)
}

func (pcq *ProductCoursesQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pcq); err != nil {
				return err
			}
		}
	}
	for _, f := range pcq.ctx.Fields {
		if !productcourses.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pcq.path != nil {
		prev, err := pcq.path(ctx)
		if err != nil {
			return err
		}
		pcq.sql = prev
	}
	return nil
}

func (pcq *ProductCoursesQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProductCourses, error) {
	var (
		nodes       = []*ProductCourses{}
		_spec       = pcq.querySpec()
		loadedTypes = [2]bool{
			pcq.withNodeC != nil,
			pcq.withNodeL != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProductCourses).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProductCourses{config: pcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pcq.withNodeC; query != nil {
		if err := pcq.loadNodeC(ctx, query, nodes, nil,
			func(n *ProductCourses, e *Product) { n.Edges.NodeC = e }); err != nil {
			return nil, err
		}
	}
	if query := pcq.withNodeL; query != nil {
		if err := pcq.loadNodeL(ctx, query, nodes, nil,
			func(n *ProductCourses, e *Product) { n.Edges.NodeL = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pcq *ProductCoursesQuery) loadNodeC(ctx context.Context, query *ProductQuery, nodes []*ProductCourses, init func(*ProductCourses), assign func(*ProductCourses, *Product)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ProductCourses)
	for i := range nodes {
		fk := nodes[i].ProductID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(product.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "product_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (pcq *ProductCoursesQuery) loadNodeL(ctx context.Context, query *ProductQuery, nodes []*ProductCourses, init func(*ProductCourses), assign func(*ProductCourses, *Product)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ProductCourses)
	for i := range nodes {
		fk := nodes[i].ProductID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(product.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "product_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pcq *ProductCoursesQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pcq.querySpec()
	_spec.Node.Columns = pcq.ctx.Fields
	if len(pcq.ctx.Fields) > 0 {
		_spec.Unique = pcq.ctx.Unique != nil && *pcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pcq.driver, _spec)
}

func (pcq *ProductCoursesQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(productcourses.Table, productcourses.Columns, sqlgraph.NewFieldSpec(productcourses.FieldID, field.TypeInt64))
	_spec.From = pcq.sql
	if unique := pcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pcq.path != nil {
		_spec.Unique = true
	}
	if fields := pcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, productcourses.FieldID)
		for i := range fields {
			if fields[i] != productcourses.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if pcq.withNodeC != nil {
			_spec.Node.AddColumnOnce(productcourses.FieldProductID)
		}
		if pcq.withNodeL != nil {
			_spec.Node.AddColumnOnce(productcourses.FieldProductID)
		}
	}
	if ps := pcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pcq *ProductCoursesQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pcq.driver.Dialect())
	t1 := builder.Table(productcourses.Table)
	columns := pcq.ctx.Fields
	if len(columns) == 0 {
		columns = productcourses.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pcq.sql != nil {
		selector = pcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pcq.ctx.Unique != nil && *pcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pcq.predicates {
		p(selector)
	}
	for _, p := range pcq.order {
		p(selector)
	}
	if offset := pcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProductCoursesGroupBy is the group-by builder for ProductCourses entities.
type ProductCoursesGroupBy struct {
	selector
	build *ProductCoursesQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pcgb *ProductCoursesGroupBy) Aggregate(fns ...AggregateFunc) *ProductCoursesGroupBy {
	pcgb.fns = append(pcgb.fns, fns...)
	return pcgb
}

// Scan applies the selector query and scans the result into the given value.
func (pcgb *ProductCoursesGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pcgb.build.ctx, ent.OpQueryGroupBy)
	if err := pcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProductCoursesQuery, *ProductCoursesGroupBy](ctx, pcgb.build, pcgb, pcgb.build.inters, v)
}

func (pcgb *ProductCoursesGroupBy) sqlScan(ctx context.Context, root *ProductCoursesQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pcgb.fns))
	for _, fn := range pcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pcgb.flds)+len(pcgb.fns))
		for _, f := range *pcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProductCoursesSelect is the builder for selecting fields of ProductCourses entities.
type ProductCoursesSelect struct {
	*ProductCoursesQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pcs *ProductCoursesSelect) Aggregate(fns ...AggregateFunc) *ProductCoursesSelect {
	pcs.fns = append(pcs.fns, fns...)
	return pcs
}

// Scan applies the selector query and scans the result into the given value.
func (pcs *ProductCoursesSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pcs.ctx, ent.OpQuerySelect)
	if err := pcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProductCoursesQuery, *ProductCoursesSelect](ctx, pcs.ProductCoursesQuery, pcs, pcs.inters, v)
}

func (pcs *ProductCoursesSelect) sqlScan(ctx context.Context, root *ProductCoursesQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pcs.fns))
	for _, fn := range pcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
