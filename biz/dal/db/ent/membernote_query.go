// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"saas/biz/dal/db/ent/member"
	"saas/biz/dal/db/ent/membernote"
	"saas/biz/dal/db/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// MemberNoteQuery is the builder for querying MemberNote entities.
type MemberNoteQuery struct {
	config
	ctx        *QueryContext
	order      []membernote.OrderOption
	inters     []Interceptor
	predicates []predicate.MemberNote
	withNotes  *MemberQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MemberNoteQuery builder.
func (mnq *MemberNoteQuery) Where(ps ...predicate.MemberNote) *MemberNoteQuery {
	mnq.predicates = append(mnq.predicates, ps...)
	return mnq
}

// Limit the number of records to be returned by this query.
func (mnq *MemberNoteQuery) Limit(limit int) *MemberNoteQuery {
	mnq.ctx.Limit = &limit
	return mnq
}

// Offset to start from.
func (mnq *MemberNoteQuery) Offset(offset int) *MemberNoteQuery {
	mnq.ctx.Offset = &offset
	return mnq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mnq *MemberNoteQuery) Unique(unique bool) *MemberNoteQuery {
	mnq.ctx.Unique = &unique
	return mnq
}

// Order specifies how the records should be ordered.
func (mnq *MemberNoteQuery) Order(o ...membernote.OrderOption) *MemberNoteQuery {
	mnq.order = append(mnq.order, o...)
	return mnq
}

// QueryNotes chains the current query on the "notes" edge.
func (mnq *MemberNoteQuery) QueryNotes() *MemberQuery {
	query := (&MemberClient{config: mnq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mnq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mnq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(membernote.Table, membernote.FieldID, selector),
			sqlgraph.To(member.Table, member.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, membernote.NotesTable, membernote.NotesColumn),
		)
		fromU = sqlgraph.SetNeighbors(mnq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first MemberNote entity from the query.
// Returns a *NotFoundError when no MemberNote was found.
func (mnq *MemberNoteQuery) First(ctx context.Context) (*MemberNote, error) {
	nodes, err := mnq.Limit(1).All(setContextOp(ctx, mnq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{membernote.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mnq *MemberNoteQuery) FirstX(ctx context.Context) *MemberNote {
	node, err := mnq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MemberNote ID from the query.
// Returns a *NotFoundError when no MemberNote ID was found.
func (mnq *MemberNoteQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = mnq.Limit(1).IDs(setContextOp(ctx, mnq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{membernote.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mnq *MemberNoteQuery) FirstIDX(ctx context.Context) int64 {
	id, err := mnq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MemberNote entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MemberNote entity is found.
// Returns a *NotFoundError when no MemberNote entities are found.
func (mnq *MemberNoteQuery) Only(ctx context.Context) (*MemberNote, error) {
	nodes, err := mnq.Limit(2).All(setContextOp(ctx, mnq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{membernote.Label}
	default:
		return nil, &NotSingularError{membernote.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mnq *MemberNoteQuery) OnlyX(ctx context.Context) *MemberNote {
	node, err := mnq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MemberNote ID in the query.
// Returns a *NotSingularError when more than one MemberNote ID is found.
// Returns a *NotFoundError when no entities are found.
func (mnq *MemberNoteQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = mnq.Limit(2).IDs(setContextOp(ctx, mnq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{membernote.Label}
	default:
		err = &NotSingularError{membernote.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mnq *MemberNoteQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := mnq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MemberNotes.
func (mnq *MemberNoteQuery) All(ctx context.Context) ([]*MemberNote, error) {
	ctx = setContextOp(ctx, mnq.ctx, ent.OpQueryAll)
	if err := mnq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MemberNote, *MemberNoteQuery]()
	return withInterceptors[[]*MemberNote](ctx, mnq, qr, mnq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mnq *MemberNoteQuery) AllX(ctx context.Context) []*MemberNote {
	nodes, err := mnq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MemberNote IDs.
func (mnq *MemberNoteQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if mnq.ctx.Unique == nil && mnq.path != nil {
		mnq.Unique(true)
	}
	ctx = setContextOp(ctx, mnq.ctx, ent.OpQueryIDs)
	if err = mnq.Select(membernote.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mnq *MemberNoteQuery) IDsX(ctx context.Context) []int64 {
	ids, err := mnq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mnq *MemberNoteQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mnq.ctx, ent.OpQueryCount)
	if err := mnq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mnq, querierCount[*MemberNoteQuery](), mnq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mnq *MemberNoteQuery) CountX(ctx context.Context) int {
	count, err := mnq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mnq *MemberNoteQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mnq.ctx, ent.OpQueryExist)
	switch _, err := mnq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mnq *MemberNoteQuery) ExistX(ctx context.Context) bool {
	exist, err := mnq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MemberNoteQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mnq *MemberNoteQuery) Clone() *MemberNoteQuery {
	if mnq == nil {
		return nil
	}
	return &MemberNoteQuery{
		config:     mnq.config,
		ctx:        mnq.ctx.Clone(),
		order:      append([]membernote.OrderOption{}, mnq.order...),
		inters:     append([]Interceptor{}, mnq.inters...),
		predicates: append([]predicate.MemberNote{}, mnq.predicates...),
		withNotes:  mnq.withNotes.Clone(),
		// clone intermediate query.
		sql:  mnq.sql.Clone(),
		path: mnq.path,
	}
}

// WithNotes tells the query-builder to eager-load the nodes that are connected to
// the "notes" edge. The optional arguments are used to configure the query builder of the edge.
func (mnq *MemberNoteQuery) WithNotes(opts ...func(*MemberQuery)) *MemberNoteQuery {
	query := (&MemberClient{config: mnq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mnq.withNotes = query
	return mnq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MemberNote.Query().
//		GroupBy(membernote.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mnq *MemberNoteQuery) GroupBy(field string, fields ...string) *MemberNoteGroupBy {
	mnq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MemberNoteGroupBy{build: mnq}
	grbuild.flds = &mnq.ctx.Fields
	grbuild.label = membernote.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.MemberNote.Query().
//		Select(membernote.FieldCreatedAt).
//		Scan(ctx, &v)
func (mnq *MemberNoteQuery) Select(fields ...string) *MemberNoteSelect {
	mnq.ctx.Fields = append(mnq.ctx.Fields, fields...)
	sbuild := &MemberNoteSelect{MemberNoteQuery: mnq}
	sbuild.label = membernote.Label
	sbuild.flds, sbuild.scan = &mnq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MemberNoteSelect configured with the given aggregations.
func (mnq *MemberNoteQuery) Aggregate(fns ...AggregateFunc) *MemberNoteSelect {
	return mnq.Select().Aggregate(fns...)
}

func (mnq *MemberNoteQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mnq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mnq); err != nil {
				return err
			}
		}
	}
	for _, f := range mnq.ctx.Fields {
		if !membernote.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mnq.path != nil {
		prev, err := mnq.path(ctx)
		if err != nil {
			return err
		}
		mnq.sql = prev
	}
	return nil
}

func (mnq *MemberNoteQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MemberNote, error) {
	var (
		nodes       = []*MemberNote{}
		_spec       = mnq.querySpec()
		loadedTypes = [1]bool{
			mnq.withNotes != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MemberNote).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MemberNote{config: mnq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mnq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := mnq.withNotes; query != nil {
		if err := mnq.loadNotes(ctx, query, nodes, nil,
			func(n *MemberNote, e *Member) { n.Edges.Notes = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (mnq *MemberNoteQuery) loadNotes(ctx context.Context, query *MemberQuery, nodes []*MemberNote, init func(*MemberNote), assign func(*MemberNote, *Member)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*MemberNote)
	for i := range nodes {
		fk := nodes[i].MemberID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(member.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "member_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (mnq *MemberNoteQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mnq.querySpec()
	_spec.Node.Columns = mnq.ctx.Fields
	if len(mnq.ctx.Fields) > 0 {
		_spec.Unique = mnq.ctx.Unique != nil && *mnq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mnq.driver, _spec)
}

func (mnq *MemberNoteQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(membernote.Table, membernote.Columns, sqlgraph.NewFieldSpec(membernote.FieldID, field.TypeInt64))
	_spec.From = mnq.sql
	if unique := mnq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mnq.path != nil {
		_spec.Unique = true
	}
	if fields := mnq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, membernote.FieldID)
		for i := range fields {
			if fields[i] != membernote.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if mnq.withNotes != nil {
			_spec.Node.AddColumnOnce(membernote.FieldMemberID)
		}
	}
	if ps := mnq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mnq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mnq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mnq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mnq *MemberNoteQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mnq.driver.Dialect())
	t1 := builder.Table(membernote.Table)
	columns := mnq.ctx.Fields
	if len(columns) == 0 {
		columns = membernote.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mnq.sql != nil {
		selector = mnq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mnq.ctx.Unique != nil && *mnq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range mnq.predicates {
		p(selector)
	}
	for _, p := range mnq.order {
		p(selector)
	}
	if offset := mnq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mnq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MemberNoteGroupBy is the group-by builder for MemberNote entities.
type MemberNoteGroupBy struct {
	selector
	build *MemberNoteQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mngb *MemberNoteGroupBy) Aggregate(fns ...AggregateFunc) *MemberNoteGroupBy {
	mngb.fns = append(mngb.fns, fns...)
	return mngb
}

// Scan applies the selector query and scans the result into the given value.
func (mngb *MemberNoteGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mngb.build.ctx, ent.OpQueryGroupBy)
	if err := mngb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MemberNoteQuery, *MemberNoteGroupBy](ctx, mngb.build, mngb, mngb.build.inters, v)
}

func (mngb *MemberNoteGroupBy) sqlScan(ctx context.Context, root *MemberNoteQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mngb.fns))
	for _, fn := range mngb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mngb.flds)+len(mngb.fns))
		for _, f := range *mngb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mngb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mngb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MemberNoteSelect is the builder for selecting fields of MemberNote entities.
type MemberNoteSelect struct {
	*MemberNoteQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mns *MemberNoteSelect) Aggregate(fns ...AggregateFunc) *MemberNoteSelect {
	mns.fns = append(mns.fns, fns...)
	return mns
}

// Scan applies the selector query and scans the result into the given value.
func (mns *MemberNoteSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mns.ctx, ent.OpQuerySelect)
	if err := mns.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MemberNoteQuery, *MemberNoteSelect](ctx, mns.MemberNoteQuery, mns, mns.inters, v)
}

func (mns *MemberNoteSelect) sqlScan(ctx context.Context, root *MemberNoteQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mns.fns))
	for _, fn := range mns.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mns.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mns.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
